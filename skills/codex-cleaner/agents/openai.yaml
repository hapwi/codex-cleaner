interface:
  display_name: "Codex Cleaner"
  short_description: "Guided Codex cleanup, history, and restore"
  default_prompt: "Use $codex-cleaner to audit Codex local state, show the recommended cleanup preset, then apply only the cleanup or restore choice I approve."
